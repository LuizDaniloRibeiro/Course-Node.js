const yourNotes = "Your notes is very good!"

const notes (n) => {
    if(n > 5 && n < 7){
        return good = "Very good! but need to study more"
    }
    else
    {
        if(n < 5)
        {
            return bad = "terrible"
        }
        else
        {
            if(n > 7)
            {
                return VeryGood = "Very Good!!!"
            }
        }
    }
}

module.exports = notes;